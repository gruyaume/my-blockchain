apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: my-blockchain-ingress
  namespace: dev1
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: my-blockchain
            port:
              number: 5000
